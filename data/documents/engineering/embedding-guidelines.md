\# Embedding Guidelines



Embeddings represent documents and queries as vectors that can be compared using semantic similarity.



The same embedding model configuration should be used consistently when comparing document and query representations.



Changing the embedding model can change similarity scores and retrieval rankings.



Embedding quality should be evaluated using the same golden dataset used to evaluate the retrieval system.



Embedding model changes should be treated as experiments rather than assumed improvements.

